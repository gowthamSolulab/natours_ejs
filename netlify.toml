# Settings in the [build] context are global and are applied to all contexts
# unless otherwise overridden by more specific contexts.
[build]
  # Directory to change to before starting a build.
  # This is where we will look for package.json/.nvmrc/etc.
  # If not set, defaults to the root directory.


  # Directory that contains the deploy-ready HTML files and assets generated by
  # the build. This is relative to the base directory if one has been set, or the
  # root directory if a base has not been set. This sample publishes the
  # directory located at the absolute path "root/project/build-output"


  # Default build command.
  command = "npm run build:js"

[[plugins]]
  # Installs the Lighthouse Build Plugin for all deploy contexts
  package = "@netlify/plugin-lighthouse"

# Production context: all deploys from the Production branch set in your site’s
# Branches settings in the UI will inherit these settings.
[context.production]
  
  command = "npm run build:js"
  environment = { NODE_ENV = "development", PORT = "3000" , DATABASE = "mongodb+srv://gowtham:<PASSWORD>@cluster0.uxnyh.mongodb.net/natours?retryWrites=true&w=majority" DATABASE_PASSWORD = "jhonny", JWT_SECRET = "my-ultra-secure-and-ultra-long-secret", JWT_EXPIRES_IN = "90d", JWT_COOKIE_EXPIRES_IN = "90" }
